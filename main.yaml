---
- name: Laboratorio Socius
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    Edad: ''
    nombre: ''

  tasks:
    - name: Imprimir la salida
      debug:
        msg: "Mi nombre es: {{ nombre }} y tengo la siguiente Edad: {{ Edad }}"
